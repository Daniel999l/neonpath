# NeonPath

AI career & college roadmap generator for students.

## Setup

### Backend
1. `npm run install:all`
2. Create `.env` file with `GROQ_API_KEY`
3. `npm run dev`

### Deployment (Vercel)
1. Set `GROQ_API_KEY` in Vercel dashboard
2. `vercel deploy`